# Workplace Evidence Overrides

Use `workplace_overrides.json` only when Cleo or the repository owner supplies direct evidence that a specific posting is Remote, Hybrid, or Onsite and the public posting omits or conflicts with that evidence.

Each override must identify the exact job URL or native ID, the confirmed mode, the evidence source, and an expiration date. Search-filter membership is never workplace evidence.
